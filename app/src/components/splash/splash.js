(function (){	'use strict';	var splash = function ()	{		return{			scope: {},			restrict: 'A',			templateUrl: 'src/components/splash/splash.html',			replace: true,			controller: 'splashController',			controllerAs: 'splash',			bindToController: true,			link: function (scope, element, attributes)			{				scope.$on('image::loaded', function (event, name)				{					if (name == 'fader')					{						scope.splash.imageLoaded();					}				});			}		};	};	angular.module('portfolio').directive('splash', [splash]);})();